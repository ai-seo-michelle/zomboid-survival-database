export type BaseRecord = {
  slug: string;
  name: string;
  location: string;
  safety: string;
  resources: string;
  difficulty: string;
  recommendedFor: string;
  description: string;
  guideLink: string;
};

